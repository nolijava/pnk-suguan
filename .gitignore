# dependencies
node_modules/

# next build output
.next/
out/
next-env.d.ts

# playwright smoke suite (isolated dev-server build dir, artifacts, session)
.next-e2e/
playwright-report/
test-results/
e2e/.auth/

# L3 packaged Windows build (build artifact, rebuilt via `npm run package:win`)
dist/

# Release binaries are published as GitHub Release assets, not git objects —
# GitHub hard-blocks files over 100 MB on push and the EXE/ZIP payloads are
# 85-250 MB. Checksums/manifests stay tracked so every asset is verifiable.
release/*.zip
release/**/*.zip
release/**/*.exe

# portable toolchain (Node.js) — machine-local, not part of the repo
.tools/

# logo generator scratch render (`npm run logo`) — the SHIPPED logo is the
# hand-supplied artwork in public/logo/, which the generator never writes to
logo-render/

# embedded postgres binaries and data directories
.pg/

# environment / secrets — NEVER commit
.env
.env.local
.env.*.local

# local database data
pgdata/

# logs
*.log

# misc
.DS_Store
*.tsbuildinfo

# assistant/tooling workspace (project id, run docs)
.freebuff/
